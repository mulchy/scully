var assert = require('chai').assert;
describe('A fake test to see if CI works', () =>
  describe('When I have an app, I can write some real tests', () =>
    it('always passes', () => assert.equal(2 + 2, 4))
  )
);

